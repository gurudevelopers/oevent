package com.sendmystatus.oeventapp.di

/**
 * Module for handling storage and persistence dependencies.
 * Currently, TokenStorage is provided via the SharedComponent constructor.
 */
interface StorageModule {
    companion object
}
